def main():
    numbers = []
    
    while True:
        try:
            num = int(input("Enter a number (or non-number to stop): "))
            numbers.append(num)
        except ValueError:
            break
    
    if numbers:
        print(f"\nFinal highest number: {max(numbers)}")

if __name__ == "__main__":
    main()
